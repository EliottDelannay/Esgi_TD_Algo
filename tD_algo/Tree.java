package tD_algo;

public class Tree {
	private Node root;
	
	
	private void buildTree(List Node) {
		
	}
	
	private void buildStaticTree(){
	    Node a = new Node('a');
	    Node b = new Node('b');
	    Node c = new Node('c', false);
	    Node d = new Node('d');
	    Node e = new Node('e');
	    Node f = new Node('f', false);
	    Node g = new Node('g');
	    Node h = new Node('h');
	    Node i = new Node('i');
	    Node j = new Node('j', false);
	    Node k = new Node('k', false);
	    Node l = new Node('l', false);
	    Node m = new Node('m');
	    Node n = new Node('n', false);
	    Node o = new Node('o', false);
	    Node p = new Node('p',false);
	}
	
	
	
	
	private void readLevelOrder(List Node ) {
	    children = []
	            while (Node !empty) {
	                   children.extend(t.children)
	                    for ch in t.children:
	                        children.extend(levelorder_visit(ch))
	            }

	            return children;
	}
	
	private void readInOrder(List Node ) {
		
	}
	
	private void readPreOrder(List Node) {
		
	}
	
	private void readPostOrder(List Node) {
		
	}

	
}
